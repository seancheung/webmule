export const QueryLimit = 100;

export const DefaultPageSize = 20;

export const enum ConfigKey {
  Theme = "theme",
  LastConnectedServer = "last.server.id",
  FileSortKey = "file.sort.key",
  FileSortDesc = "file.sort.desc",
  PageSize = "page.size",
}
